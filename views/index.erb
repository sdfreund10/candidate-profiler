<div class="hero">
  <h1 class="text-center">Research any candidate <span class="highlight">in minutes.</span></h1>
  <p class="tagline text-center">Paste a campaign website URL and get an instant profile of their background, positions, and endorsements.</p>

  <form method="post" action="/profile">
    <div class="search-row">
      <input type="url" id="campaign_url" name="campaign_url" placeholder="https://candidate-website.com" value="<%= ERB::Util.html_escape(@campaign_url.to_s) %>" autofocus>
      <button type="submit">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"/>
          <path d="m21 21-4.35-4.35"/>
        </svg>
        Research
      </button>
    </div>
    <p class="hint text-center">Paste your candidate's official campaign website URL</p>
    <% if @error %>
      <p class="error text-center"><%= ERB::Util.html_escape(@error) %></p>
    <% end %>
  </form>
</div>
